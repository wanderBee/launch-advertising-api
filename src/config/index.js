const devConfig = {
    host: 'localhost',
    port: '3030'
};

const proConfig = {
    host: '120.79.216.186',
    port: '3030'
}

module.exports = {
    development: devConfig,
    production: proConfig
}